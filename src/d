    <>
    {/* {data.map(fetchData)} */}
      <div className="bg-white rounded-xl shadow-md overflow-hidden hover:shadow-xl transition duration-300">
        {/* Product Image */}
        <div className="h-64 flex items-center justify-center bg-white p-4">
          <img
            src={data.image}
            alt={data.title}
            className="h-full w-full object-contain"
          />
        </div>

        {/* Product Information */}
        <div className="px-5 pb-5">
          {/* Product Name */}
          <h2 className="text-xl font-bold text-gray-900 truncate">
            {data.title}
          </h2>

          {/* Brand */}
          <p className="text-gray-500 text-lg mt-2">{data.brand}</p>

          {/* Price */}
          <p className="text-[#43978e] text-xl font-bold mt-2">${data.price}</p>

          {/* Rating */}
          <p className="text-gray-500 text-lg mt-1">⭐ {data.rating}</p>

          {/* Add To Cart */}
          <button className="w-full mt-3 bg-[#5485a8] hover:bg-[#467796] text-white text-xl py-3 rounded-lg transition">
            Add To Cart
          </button>
        </div>
      </div>
      
    </>